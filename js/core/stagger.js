/// Todo rename this to a function that takes a square area
// and then staggers it somehow
class staggered_area {
    area;
    staggeredArea;
    constructor(area) {
        this.area = area;
        // 
    }
    build_stagger() {
    }
}
export default staggered_area;
